# セキュリティ

## 報告の方法

脆弱性を見つけた場合は、**公開の Issue を立てずに** GitHub の
[Security Advisories](../../security/advisories/new) から非公開で報告してください。

受け取ったら確認し、内容と対応の見込みを返します。返答までにかかる期間は約束できません
(作者1人が空き時間で見ているためです)。報奨金の制度はありません。

修正を公開するときに、希望があれば報告者として名前を載せます。

## このアプリが触れるもの

- データは実行している端末の SQLite ファイル1つに入ります。外部へは送りません。
- ネットワーク通信を行いません。サーバーもアカウントもありません。
- 画面を描く側(renderer)は、`sandbox: true` / `contextIsolation: true` /
  `nodeIntegration: false` のもとで動きます。ファイルと DB に触れるのは main 側だけで、
  renderer からの要求は `src/main/ipc/validate.ts` で実行時に検証してから通します。

したがって、現実的な攻撃面は次のあたりです。

- **依存パッケージと Electron 本体** —— このリポジトリのコードより、こちらのほうが広いです。
- **開いた DB ファイル** —— 他所から受け取った `.db` を開く使い方は想定していません。
- **将来の MCP(AI からの操作)** —— まだ実装していません。実装した時点で、外部のプロセスが
  グラフを書き換える経路ができます。

## 対象のバージョン

まだリリースしていません。配布ビルドもありません。対象は `main` の現在の内容です。

# 要件定義 v1

**この文書が現行の仕様である。** 実装との食い違いを見つけたら、どちらかが間違っている。

背景と問題設定は `README.md`、設計判断とその代償は `design/` を参照。
なぜこの製品が要るかの調査は `research/` にある。

## 用語

- **Task**: 作業の単位。0個以上の **childTask** を持てる。**深さは1まで**(childTask はさらに子を持てない)。
- **childTask**: Task を分解した項目。フラットなチェックリストとして扱う。依存グラフには参加しない。
- **依存エッジ**: トップレベルの Task 同士を繋ぐ有向の関係。`A → B` は「A が完了するまで B は着手できない」。
- **Project**: キャンバス上の**領域(矩形)**。Task はその中にあることで所属する。所属は保存しない。
- **軸a(計算される)**: Ready / Blocked。依存エッジから導出する。**保存しない。**
- **軸b(自己申告)**: Not Done / In Progress / Done。作業した本人が設定する。軸aとは独立。

### v1 の範囲

- **依存エッジは厳密(strict)のみ。** 緩やか(loose)な順序関係は v1 のスコープ外。
- **軸a はトップレベルの Task のみが持つ。** childTask は持たない(親から継承すべきかは未決定)。
- **Project のクラスタ折りたたみは実装しない。** v1 のスコープ外。

## 機能要件(受け入れ条件つき)

### FR-1 Taskの作成・編集・削除
- Taskを新規作成できる。
- Taskの内容(名称等)を編集できる。
- **名前の編集はワンクリックで始まる。** 表示されている名前をクリックすると、その場で入力欄になる。
  - Enter で確定する。Escape で取り消す。入力欄の外を押したときも確定する。
  - **Shift+Enter は確定に加えて childTask を1件追加し、その入力へ移る**(FR-2)。分解を続けて書き出す間、ボタンへ手を戻さずに済ませるため。
  - 日本語入力の変換確定の Enter は、確定操作として扱わない。
  - 理由: ダブルクリックや、専用の編集ボタンを経由させると、名前を直すだけの操作が2手になる。名前は最も頻繁に触る場所である。
  - Project 名の改名も同じ起点にする(FR-4)。同じ見た目の文字を、片方はワンクリック・片方はダブルクリックで編集させると、どちらだったかを覚える必要が生じる。
- Taskを削除できる。
- **削除前に、その削除によって生じる変更内容を提示し、確認を求める。** 提示する内容:
  - childTaskを持つ場合、childTaskも削除される旨
  - 削除される依存エッジの一覧
  - 再接続によって新たに作成される依存エッジの一覧(FR-3のルールによる)
  - 理由: 再接続ルールは条件によって挙動が変わり(特に入力・出力とも複数の場合は再接続されない)、ユーザーが依存関係を意図せず失う可能性がある。何が起きるかを事前に見せることで、この非対称性を許容可能にする。
  - この確認はUI操作時のもの。AI(MCP)経由の削除では確認を挟まない(AIに対する復旧手段はUndo(FR-8)である)。
- 依存エッジの再接続ルールはFR-3を参照。
- 受け入れ条件: 作成・編集・削除の結果が即座にキャンバス上の表示に反映される。

### FR-2 childTaskの追加・削除・並べ替え
- トップレベルのTaskに対し、childTaskを追加できる。
- childTaskは編集・削除ができる。
- childTaskはさらに自分のchildTaskを持てない(UI上、その操作自体が選択肢として存在しない)。
- **childTaskは親Task内で明示的な順序を持ち、ユーザーが並べ替えられる。** 1番目・2番目…という順序が保持され、表示もその順に従う。
  - 並べ替え操作もUndo対象に含める(FR-8)。
- **キーボードだけで分解を書き出し、行き過ぎたぶんを戻せる。**
  - 名前の編集中に Shift+Enter で、確定してchildTaskを1件足し、その名前の入力へ移る(FR-1)。
  - 名前の編集中に Tab で、確定してそのTask/childTaskのメモの入力へ移る(FR-10)。
    - 理由: 名前を打ち終えてメモを書きたいたびにマウスへ持ち替えるのでは、書き出しの流れが切れる。メモは名前の次にある欄なので、Tab 本来の意味からも外れない。
  - 名前が空の状態で Backspace を押すと、そのchildTaskを削除し、1つ上のchildTaskの名前の入力へ戻る。上がない場合はフォーカスを移さない。
    - 理由: Shift+Enterで足しすぎたときに、手をキーボードから離さずに取り消せること。アウトライナで広く使われている操作であり、覚え直しを要さない。
    - **この操作はchildTaskにのみ適用する。** Taskを同じ操作で削除できるようにすると、childTaskの巻き添え削除と依存エッジの再接続がFR-1の確認を経ずに走る。
    - 誤って消した場合はUndoで戻せる(FR-8)。
  - 受け入れ条件: Taskの名前を空にしてBackspaceを押しても、そのTaskは削除されない。
- **childTaskの役割の明文化**: childTaskは「Taskを分解し、見やすく整理する」ための構造であり、**ブロッキングを伴う実行順序を表現するものではない**。
  - 「この順にやる」という程度の順序は、**並び順そのものが担う**(意味は保持するが、Ready/Blockedの計算には関与しない)。
  - 「先が終わらないと着手できない」というブロッキングを表現したい場合は、**それぞれをトップレベルのTaskとして表現し、依存エッジで接続する**。
  - **ユーザーがchildTask間の接続を試みた場合の挙動を定義する**: 接続操作を単に無効にするのではなく、「childTask間には依存関係を作れないこと」と「ブロッキングを表現するにはトップレベルのTaskにする必要があること」が伝わる形で提示する。
    - 理由: この制約はモデル上は一貫しているが、ユーザーの直感には反しうる。無言で拒否すると、ユーザーは「壊れている」と解釈する。
  - この役割分担が実際にユーザーに理解されるかは**検証が必要な仮説**である。使用してみて直感に反することが判明した場合、モデルの見直し対象とする。
- 受け入れ条件: childTask を Task に追加した場合、その childTask に対して「依存エッジを張る」「Project を個別に設定する」という操作が一切提示されない。

### FR-3 依存エッジ(厳密のみ)
- トップレベルのTask間にのみ、依存エッジを作成・削除できる。
- 依存エッジはA→B(AがDoneになるまでBはBlocked)という向きを持つ。
- 接続はドラッグ&ドロップで行う(Taskの端をドラッグして矢印を伸ばし、接続先Taskの端にドロップする)。
- **依存を外す操作は、その線の上に置く。** 線に触れると外すための印が現れ、押すとその1本だけが外れる。
  - 理由: 「選んでから削除キー」に頼ると、同じキーが Task の削除も兼ねることになり、FR-1 の確認を迂回する経路が生まれる。実際、Task を選んで削除キーを押すと、Task は残ったまま**繋がっていた依存だけが黙って消える**状態になっていた。
  - **削除キー(Delete / Backspace)にグラフを変える操作を割り当てない。** 消す対象は、Task は × から(確認つき)、依存は線の上の印から、childTask は名前を空にした Backspace から。いずれも対象がその場ではっきりしている。
  - 受け入れ条件: Task を選んで Delete または Backspace を押しても、Task も依存エッジも変化しない。
- 循環依存になる接続は、ドロップしても接続が成立しない(接続時点で防止する。事後的な検知・警告は行わない)。
- Project の境界を跨いだ依存エッジも作成できる。
- **Task削除時の依存エッジ再接続ルール**: あるTaskを削除する際、それに繋がっていた依存エッジは以下のルールで扱う。
  - 入力・出力とも1本ずつ(A→B→C): 削除後、自動的にA→Cへ再接続する。
  - 入力または出力の一方のみが複数(例: A1→B, A2→B, B→C): 単数側と複数側を総当たりで再接続する(この例ならA1→C, A2→C)。
  - 入力・出力の両方が複数: 自動再接続はしない。削除対象Taskに繋がっていた依存エッジを全て削除するのみ。
  - 再接続によって既存のエッジと重複が生じる場合(例: A1→C が既に存在する状態で A1→C を再接続しようとする)、重複を作らず既存エッジを維持する(冪等に扱う)。
- 受け入れ条件: 依存エッジで結ばれたA→Bについて、Aが未Doneの間、Bは常にBlockedとして表示される。Aが手動でDoneに変更された瞬間、Bは自動的にReadyへ再計算される(手動操作不要)。

### FR-4 Project(領域)
- **Project はキャンバス上の「領域(矩形)」である。** タスクに貼るタグではない。
  - 作成すると枠が現れ、辺や角をつまんで自在に大きさを変えられる。**つまむのに枠を選択する操作を先に要求しない。**
    - 理由: 取っ手が出ていない理由が「選んでいないから」だと分かるのは仕組みを知っている側だけで、触った側には「掴めない」としか映らない。
  - **枠は内側のどこを掴んでも動かせる。** 中の Task は枠より手前にあるため、Task の上から始めたドラッグは Task 単体の移動になる。
    - 代償として、枠の内側ではドラッグによるキャンバスの平行移動ができない。平行移動はスクロールに割り当てる(FR-6)。
  - 枠を動かすと、**中に入っている Task も一緒に動く**。所属を保ったまままとめて配置を整理できるようにするため。
- **所属は「Task がその枠の中にあるか」だけで決まる。** 枠に入れば付き、外へ出せば外れる。
  - **保存しない。** Ready/Blocked と同じ導出値として扱う。保存すると、位置とラベルが食い違う状態が生まれるため。
  - 判定は Task の左上の点で行う。中心点のほうが直感には近いが、それには描画サイズが必要で、サイズは表示側の都合であってデータが持つべきものではない。
  - 枠が重なっている場合は**面積の小さいほう**に属する(より限定的なほうが意図に近いため)。面積が同じなら id の小さいほう。
  - 1つの Task が属する Project は高々1つ。領域に置ける場所は1つだけなので、構造的にそうなる。
- childTask は親 Task の所属を引き継ぐ(親の位置から導出される)。
- Project の削除・改名ができる。改名は Task 名と同じくワンクリックで始まる(FR-1)。**削除しても中の Task は消えない** —— 領域であって入れ物ではないため、枠が消えれば所属が外れるだけで、Task はその場に残る。
- 整列(FR-6)を実行したときは、Task の座標と枠の矩形をまとめて置き直す。Task だけ動かすと枠から外れて所属が消えるため。

### FR-5 状態管理(2軸)
- 軸a(Ready/Blocked)は、トップレベルのTaskについて、入力される依存エッジの状態から自動計算される。ユーザーは軸aを直接変更できない。
  - 入力される依存エッジを1本も持たないTaskは、常に**Ready**とする。
  - 入力される依存エッジを持つ場合、その全ての先行TaskがDoneであればReady、1つでも未Done(Not Done または In Progress)であればBlockedとする。
- 軸b(Not Done/In Progress/Done)は、TaskおよびchildTaskの両方に対して、ユーザーが直接設定できる。
- 軸aの値(特にBlocked)は、Taskの編集や軸bの設定を一切制限しない。
- 親Taskの軸bと、childTaskの軸bは、互いに自動連動しない。
- 受け入れ条件: あるTaskがBlockedであっても、そのTaskの名称編集・childTask追加・軸bをIn Progressに設定する、といった操作が全て通常通り行える。

### FR-6 可視化(単一キャンバス)
- 全ての Task(Project に属するかを問わず)が、単一の共有キャンバス上に表示される。
- childTaskを持つTaskは、折りたたみ・展開ができる。
  - **折りたたんでいる間は、隠れている childTask の件数を表示する。** ホバーを待たずに見せる。
    - 理由: 畳んだ Task は childTask を持たない Task と見分けがつかず、**分解したこと自体が画面から消える**。同じ内容をもう一度書き出すことになる。
- Project は矩形として描画されるが、v1 ではクラスタとしての折りたたみは実装しない — 個々の Task は常に見える。
- **完了したTaskの表示を抑制できる。** 完了済みTaskが蓄積してキャンバスを圧迫する問題に対応する。
  - 既定では、完了(Done)したTaskおよびchildTaskは**淡く・控えめに描画される**(非表示にはしない)。完了したものが見えていること自体が「なんとなくの進捗」の把握に寄与するため(Vision: 全体フローは可視化から自然に見えるべきもの)。
  - それに加えて、**「完了済みを非表示にする」トグル**を提供する。有効にすると、完了したTaskはキャンバスから描画されなくなる。
  - 非表示のTaskも依存グラフ上には存在し続ける。すなわち、隠れたDone済みTaskは後続TaskのReady判定に引き続き寄与する(隠すことでReady/Blockedの判定が変わってはならない)。
  - 非表示のTaskに接続するエッジは描画されない。結果として、**残るエッジは「まだ実際にブロックしているもの」だけになる**。これは意図された効果である。
  - この表示設定は、タスクグラフのデータではなく**アプリケーションの表示設定**として扱う。したがってUndo/Redo(FR-8)の対象外とする。
- **配置は手動を基本とする。** ユーザーがドラッグして決めた座標を常に尊重し、システムが勝手に再配置することはない。
  - **Task はノードのどこを掴んでも動かせる。** ただし名前・メモ・各ボタンの上ではクリック操作(編集・状態変更等)が優先され、そこからは移動が始まらない。
    - 理由: 掴める場所を一点に限ると、動かすたびにその一点を狙う必要があり、配置を試行錯誤する操作の負担になる。
  - 理由: Principle 2(思考をそのまま操作できること)に対応する。ユーザーが意味を持たせて置いた位置を、システムが自動整列で崩すことは、空間的な記憶と操作の主導権を奪うことになるため。
  - 例外として、ユーザーが明示的に「整列」操作を実行したときのみ、機械的なレイアウトアルゴリズムによる再配置を行う。
  - 整列操作もUndo対象に含める(FR-8)。意図しない整列から元の配置に戻せること。
  - **整列は所属(FR-4)を壊してはならない。** 具体的には、整列後に次が成り立つこと。
    - Project に属する Task が、その枠から食み出さない
    - Project の枠どうしが重ならない
    - 整列の前後で、どの Task がどの Project に属するかが変わらない
    - 理由: 所属は位置から導出される。食み出しや枠の重なりは見た目の粗さでは済まず、**そのまま所属の消失や入れ替わりになる**。
    - このため、枠の寸法は Task の実際の描画サイズから決める。決め打ちの見積もりでは、childTask やメモで背が伸びた Task が枠を突き破る。
- **キャンバスの平行移動はスクロール、拡大縮小はピンチと修飾キー+スクロールに割り当てる。**
  - 理由: 枠の内側がドラッグで移動できる(FR-4)ため、ドラッグで平行移動できる余白が減る。図を扱う道具で広く使われている割り当てでもある。
  - **掴んでの平行移動は行わない。** 掴んだ先が余白か枠かで結果が変わると、狙いを外したときに何が起きるかが読めない。移動はスクロールに一本化する。
- 受け入れ条件: 依存エッジ・Project の矩形・折りたたみ状態・ノード座標を含むグラフ全体の状態は、アプリケーションを再起動しても保持される。

### FR-7 データ保存
- 全データはローカルに保存される。保存形式は**SQLite**とする。
  - 採用理由: AIが削除・列挙・フィルタリングといったCRUD操作を日常的に行う主体であるという運用を踏まえ、(a)削除+自動再接続(FR-3)のような複数ステップの操作をトランザクションとして原子的に扱える、(b)外部キー制約により「存在しないTaskを参照するエッジ」等の参照整合性をDB側でも担保できる、(c)「Readyなものを列挙して」等の参照操作を、都度スクリプトを書かせずSQLで決定論的に行える、という3点を優先した。
  - **注**: 循環依存の防止は外部キー制約では表現できない(グラフ探索が必要)。これはアプリケーション層のロジックで担保する(FR-3、NFR-2)。
  - 人間がこのファイルを直接閲覧・編集することは想定しない(閲覧・簡単な操作はアプリのUIを通して行い、削除・列挙等はAIが担う)。そのためgit等によるテキスト差分管理は本要件の対象外とする。
- 永続化すべきデータには、Task・childTask・依存エッジ・Project の矩形・軸bの状態・メモに加え、**キャンバス上のノード配置座標と折りたたみ状態**を含む。**Project への所属は保存しない**(位置から導出する)。

### FR-7.5 AIの操作経路
- **書き込み操作(作成・更新・削除)は、必ずアプリケーション層が提供する口(MCPサーバー、CLI等 — 具体的な形式は設計フェーズで決定)を経由する。** AIがSQLiteファイルへ直接書き込むことは想定しない。
  - 理由: FR-3の削除時自動再接続、FR-8のUndo対象への記録、FR-3/NFR-2の循環依存防止は、いずれもアプリケーション層のロジックである。DBへの直接書き込みはこれらを全て迂回してしまうため。
- **参照操作も、アプリケーション層が提供するツール(`get_ready_tasks` 等)を正式な経路とする。**
  - 理由: AIに生のSQLを書かせると、AIがスキーマを把握している必要があり、スキーマ変更のたびにAIのクエリが壊れる。ドメインAPIとして提供すれば、内部構造の変更から独立した安定したインターフェースになる。
  - SQLiteへの直接SELECTは**禁止しないが、開発・デバッグ用途の抜け道**という位置づけとする。定常的な運用経路とはしない。
  - 決定論性(NFR-4)は、ツールが決定論的な結果を返すことで満たされる。
- **書き込みの直列化**: UI経由の操作とAI経由の操作が同時に発生しても、データ整合性が破れないこと。
  - 実装上は、ドメイン層とMCPサーバーを同一プロセス内に置く構成(`design/tech-stack.md`)により、書き込みは構造的に直列化される。
  - ただし**UI側での考慮は別途必要**: ユーザーがノードを操作している最中にAIがそのノードを削除する、といった状況が起こりうる。UIはグラフの外部変更に対して破綻なく追従できること。
- 受け入れ条件: AIがアプリ経由でTaskを削除した場合、(a)FR-3の再接続ルールが適用され、(b)その操作がUndo可能な単位として記録され、(c)結果が即座にUI表示へ反映される。

### FR-8 Undo / Redo
- Cmd+Zでの取り消し(Undo)、Cmd+Shift+Zでのやり直し(Redo)ができる。
- 対象は人間がUI上で行った操作に限らず、**AIが行った操作(削除、依存エッジの再接続、Task追加、軸bの変更等)も含む**。AIが予想外の操作をした場合の復旧手段として位置づける。
- 受け入れ条件: AIが会話中にTaskを削除し依存エッジが自動再接続された場合でも、Cmd+Zで削除前の状態(元の依存エッジ構成を含む)に戻せる。
- Undoの深さ(何手戻せるか)、セッションを跨いで(アプリ再起動後も)Undo可能かは**要決定**(設計フェーズで確定)。
- 「1操作」の粒度(例: AIが1回の会話で5つのTaskを削除した場合、Undo 1回で全て戻すか、5回に分けるか)も**要決定**。

### FR-10 メモ
- **Task と childTask のそれぞれ**が、自由記述のメモを持てる。空文字はメモなしを意味する。
- 改行を含む複数行の本文をそのまま保持する。
- **書き込んだ内容は、それぞれの名前のすぐ下に、小さく淡い文字で本文として表示する。**
  - 理由: 入力欄の箱として常設すると、メモを書いていない Task でも場所を取り、縦に嵩む。書いたものだけが見えればよい。
  - 表示された本文をクリックすると編集に入る。書かれていない場合は ✎ から始める。
- **書いている途中ではなく、確定(入力欄からフォーカスが外れる)時に保存する。**
  - 理由: 一文字ごとに保存すると、Undo 履歴が打鍵で埋まり FR-8 が機能しなくなる。ドラッグの座標を確定時にだけ送るのと同じ理由。
- メモがあるものは ✎ の印を常に表示する(ホバーを待たない)。
  - 理由: このメモは**中断して戻ってきたときに読み返すためのもの**であり、探しにいかないと見つからないなら役に立たない。
- 受け入れ条件: Task と childTask の双方にメモを書いてアプリを再起動しても、改行を含めて内容が保持される。

### FR-9 データのエクスポート(バックアップ)
- ユーザーが任意のタイミングで、**SQLiteのDBファイルを任意の場所へコピー保存できる**。
- 復元は、保存したファイルを元の位置へ戻すことで行う(アプリ側に取り込み機能を持たせない)。
- 自動バックアップ・クラウド同期は行わない(非ゴールを維持する)。
- **この要件を設けた理由**: 完全ローカル + SQLite + 人間がファイルを直接読まない、という構成では、データ消失に対する復旧手段が存在しない。Undo(FR-8)はユーザーやAIの操作ミスには有効だが、**ファイル破損・ディスク障害・誤削除には一切効かない**。エクスポートはこの穴を塞ぐ最小限の手段である。
- 受け入れ条件: エクスポートしたファイルを元の位置に戻して起動すると、エクスポート時点の状態が完全に復元される。

## 非機能要件

- **NFR-1 パフォーマンス**: 実運用に近い規模(数十〜百程度のTaskが同一キャンバス上に存在する状態)でも、キャンバスの操作(パン・ズーム・ドラッグ・折りたたみ)が体感的な遅延なく行えること。可視化品質はVisionで明記した通り主要な成功基準であり、性能はその前提条件である。
- **NFR-5 規模拡大時の認知負荷**: Taskが増えたとき、性能とは別に**「画面上に100個のノードがある」こと自体が問題になる**。
  - このプロダクトの価値は「全体が見えること」ではなく「**全体を見た上で、今やるべきものが分かること**」である(設計原則3)。
  - したがって、規模が大きくなるほど、Ready / Blocked / Done の視覚的な優先順位付けが重要になる。完了Taskを淡く描画する(FR-6)のはその第一歩であり、それだけで十分とは限らない。
  - v1では具体的な解決策を規定しないが、**UIの評価軸として「100 Task規模で、今着手すべきものが即座に見つかるか」を用いる**。
- **NFR-2 データ整合性**: 依存エッジの循環や、childTaskの二重の親付けなど、データモデル上あり得ない状態を作れないこと(UI操作レベルで防止するか、保存時に検証するかは設計フェーズで決定)。
- **NFR-3 可用性**: ネットワーク接続なしで全機能が動作すること(ローカル完結の要件に対応)。
- **NFR-4 AIによる決定論的なクエリ可能性**: 「未完了のTaskを列挙する」「Readyなものだけ抽出する」といった操作が、AIの読解・推測に依存せず、常に決定論的な結果を返すこと。人間が生データを直接読む・編集することは想定しない。

## 制約条件

- シングルユーザー、シングルデバイス利用を前提とする(同時編集・排他制御は考慮しない)。
- チーム機能、クラウド同期、外部チケット管理システム連携は行わない(PRDの非ゴールを継承)。

## 前提条件

- ユーザーはエンジニアであり、AIコーディングツール(Claude Code等)を日常的に利用している。
- タスクグラフの操作は、人間がUIで行う簡単な操作と、AIがアプリ経由で行う操作(削除・列挙等)の2つが併存する(FR-7.5)。ただしその際もプロダクト自体がタスクを実行するわけではない(PRD 5.2を継承)。
- 人間がSQLiteファイルの中身を直接閲覧・編集することは想定しない(FR-7)。

## v1で解決した/未解決のまま持ち越す事項

**v1範囲内で解決したもの**
- childTaskの軸a: v1では持たない(継承の要否はv1 の後で再検討)。
- 依存エッジの種類: v1では厳密のみ。緩やかは v1 のスコープ外。
- Projectクラスタ折りたたみ: v1では実装しない。
- Task削除時の挙動(FR-1): 警告の上でchildTaskごと削除。
- 依存エッジの再接続ルール(FR-3): 単純な1-1の場合は自動再接続、片側のみ複数の場合は総当たりで再接続、両側とも複数の場合は自動再接続せず削除のみ。
- 循環依存の防止(FR-3): ドラッグ&ドロップによる接続時点で、循環になる場合は接続自体が成立しない。
- 保存形式(FR-7): SQLiteを採用。理由はAIが日常的にCRUD操作を行う運用を前提とした、トランザクションの原子性・参照整合性の担保・決定論的クエリの3点。人間による生データの直接閲覧・編集、git等によるテキスト差分管理は想定しない。
- AIの操作経路(FR-7.5): 書き込みはアプリケーション層経由に限定し、参照(SELECT)のみ直接SQLを許可する。理由は、削除時の自動再接続・Undoへの記録・循環依存防止がいずれもアプリ層のロジックであり、DBへの直接書き込みはこれらを迂回してしまうため。
- Undo/Redo(FR-8): Cmd+Z/Cmd+Shift+Zで、人間・AIどちらの操作も対象に含めて戻せる。
- 配置方針(FR-6): 手動配置を基本とし、システムによる自動再配置は行わない。ユーザーが明示的に「整列」を実行したときのみレイアウトアルゴリズムを適用する(この整列操作もUndo対象)。

- 外部レビュー(2026-08)を受けて追加・修正したもの:
  - 削除時に変更内容を提示する確認UI(FR-1)
  - childTaskの役割の明文化と、接続を試みた際の挙動(FR-2)
  - Project に属せるのが高々1つである理由の明記(FR-4)
  - AIの参照経路をドメインAPI中心に変更、直接SELECTは開発用の抜け道へ格下げ(FR-7.5)
  - UI操作とAI操作の同時実行に関する要件(FR-7.5)
  - エクスポート機能をv1に追加(FR-9)
  - 規模拡大時の認知負荷を非機能要件に追加(NFR-5)

**設計フェーズまでに決めるべきもの(要決定)**
- AIがアプリ経由でTaskを新規作成した際の、キャンバス上の初期配置座標の決定方法(FR-6, FR-7)。
- グラフの外部変更(AI操作)に対して、UIがどう追従するか(FR-7.5)。特にユーザーが操作中の要素が変更・削除された場合。

**設計フェーズで決定済み(`design/` 配下を参照)**
- アプリケーションの実行形態: Electron + React + TypeScript(`design/tech-stack.md`)
- AIがアクセスする口の形式: MCPサーバー(mainプロセス内)(`design/tech-stack.md`)
- Undoの深さ50・セッション跨ぎ非対応・粒度はドメインAPI 1回の呼び出し(`design/domain-design.md`)

**v1 の後へ持ち越すもの(本ドキュメントでは扱わない)**
- 緩やかな依存エッジ、Projectクラスタ折りたたみとエッジ再描画、childTaskの軸a継承、進捗ログ、期限・工数ベースの完了率。

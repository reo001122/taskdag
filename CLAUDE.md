# CLAUDE.md

このリポジトリで変更を加えるときの取り決め。

**詳細は各文書にある。ここに置いているのは、破ると壊れるものと、破ったことが後から
分かりにくいものだけ。** 網羅ではない。

- 設計判断とその代償 → `design/decisions.md`
- 規約の詳細 → `design/coding-standards.md`
- 現行の仕様 → `requirements.md`

---

## 1. 壊してはいけない境界

### ドメイン層は外部を import しない

`src/main/domain/` の**実装ファイル**は、Electron も `node:sqlite` も React も Node 標準
モジュールも import しない。純粋な TypeScript として保つ。

理由は要件由来。**すべてのグラフ操作**(UI から、将来は MCP 経由で AI から)が同じコードを
通る必要があり、そうして初めて依存のルール・循環防止・Undo がどちらにも効く。

**検査**: `src/main/domain/boundary.test.ts` が落とす。規約に書くだけでは import を1行
足すだけで静かに破れるため、テストにしてある。テストファイル自身は `vitest` を import
するので対象外。

### タスクグラフへの書き込みは `AppService.mutate` を通す

グラフを変える操作は、renderer からも(将来の)MCP からも `AppService.mutate` を経由する。
再接続ルール・循環防止・Undo への記録・永続化失敗時の巻き戻しが、すべてここに集約されている。

**明示された例外**: 表示設定(`app_settings` テーブル)。`ipc/handlers.ts` が
`db/settings.ts` を直接呼ぶ。表示設定はタスクグラフのデータではなく、Undo の対象外である
ため意図的に外してある。**これ以外に db への直接の書き込み経路を作らない。**

### 永続化に失敗したらメモリを巻き戻す

`AppService.mutate` は、DB への書き込みに失敗したら DB を読み直してメモリ状態を戻し、
Undo 履歴を破棄する。**DB を唯一の正とする。**

巻き戻さないと、書き込めなかった変更がメモリに残り、それが次回以降の差分計算の基準になる。
結果として失敗した変更は二度と再試行されず、再起動時に無言で消える。
**この経路を迂回する永続化を追加しない。**

## 2. 変更したら一緒に直すもの

### IPC のコマンドを追加・変更したら3箇所

`shared/ipc.ts` の型を足すだけでは通らない。**renderer から届く値は実行時には型がない。**

1. `shared/ipc.ts` — 型
2. `ipc/validate.ts` — **実行時の検証**。ここが唯一の関門
3. `ipc/handlers.ts` — ドメインへの受け渡し

検証を怠ると、不正な値がドメインを素通りして SQLite の CHECK 制約で初めて弾かれ、
その時点でメモリは既に変わっている(上記の巻き戻しが走る)。手前で止めるほうが安い。

### SQLite のスキーマを変えたら

`db/schema.ts` に **migration を追加**し、`SCHEMA_VERSION` を上げる。既存の DB からの
アップグレードが通ることを確認する。

**順序の制約**: SQLite は列を落とす際に依存ビューを検証し、索引のある列は落とせない。
「ビュー → インデックス → 列」の順で処理する。実際に migration 2 と 5 で踏んでいる。

### 仕様が変わったら `requirements.md`

**`requirements.md` が唯一の規範文書。** 他の文書がこれと食い違ったら、他の文書が古い。

過去に、仕様を述べる文書が複数並び、それぞれ違うことを書いていた(一方は「AI が DB へ
直接書き込む」前提のまま、もう一方では逆転していた)。**どれが正か判別できない**状態に
なったため、`requirements.md` 以外をこのリポジトリから外した。

**仕様を述べる文書を増やさない。** 増やすなら、どちらが正かを先に決める。

設計判断が変わったら `design/decisions.md` に**何を却下し何を失ったか**を追記する。

## 3. 自動チェックが捕まえないもの

**型チェックもテストも通るのに画面が壊れている、という状態が何度も起きた** ——
ノードがカーソルに付いてこない、タイトルが編集できない、枠線が3つの角で途切れている。
自動チェックはこれらを1つも捕まえなかった。

キャンバスに触れる変更をしたら、**アプリを開いて実際に使う。** 具体的な手順は
`CONTRIBUTING.md` の「自動チェックが捕まえないもの」にある。

**「何も起きない」形で出る不具合は、目で見ても原因が分からない。** 入力欄は出ているのに
フォーカスが移っていない、枠が前面に出ていて中の Task に触れない —— いずれも見た目は
正常だった。この2つには道具がある。

- `npm run dev -- -- --log=debug` — 起きなかったことをログに残す(`src/shared/log.ts`)
- `npm run probe` — 本物のキーとマウスを送って、その後の DOM を読む(`tools/ui-probe/`)

**推測で直す前に観測する。** 実際に、静的に読んで立てた仮説は2回とも外れた。

そのため **`main` へ直接コミットせず**、feature ブランチで作業して確認を経てからマージする。
(このルールは 2026-09 に決めたもので、それ以前の履歴には適用されていない。)

## 4. 設計上の癖

新しく書くときに従うもの。理由は `design/decisions.md` にある。

- **導出できるものは保存しない。** Ready/Blocked も Project への所属も計算する。
  保存すると更新漏れによる不整合が必ず起きる。導出なら不整合が**表現不可能**になる。
  ただし SQL 側にも同じ導出を書くことになるため、**ドメイン関数と一致することをテストで
  確認する**(`db/views.test.ts`)。
- **確定していない状態を Undo 履歴に積まない。** ドラッグ中の座標、入力中の1文字ごとの
  変更を積むと、履歴が中間状態で埋まって Cmd+Z が使い物にならなくなる。
  確定時(マウスを離す、フォーカスが外れる)に1回だけ送る。
- **要件由来の判断にはコメントで要件番号を書く**(`// FR-3: 入力・出力の両方が複数の場合は
  再接続しない`)。後から「この分岐は消していいのか」を判断するとき、要件に遡れる。
  ドメイン層のテスト名にも同様に付けたいが、**現状は徹底できていない**(86件中13件)。
  新しく書くテストには付ける。

## 5. 文書に書くこと・書かないこと

### 公開する(このリポジトリ)

| 文書 | 書くこと |
|---|---|
| `README.md` | 見知らぬ読者が「自分向きか」を判断するための情報 |
| `requirements.md` | **現行の仕様。** 何がどう振る舞うべきか、受け入れ条件つき |
| `design/*.md` | どう作られているか |
| `design/decisions.md` | 却下した案と、各判断が何を犠牲にしたか |
| `research/` | 一次情報にあたった調査。事実・根拠・未解決の問いを分ける |
| `CONTRIBUTING.md` | 外部の人が変更を提案するために必要なこと(内容の一部は他文書と重複してよい) |

### ここに置かないもの

ブレストの経過、その時点の判断の記録、QA の手控え、配布の手順、受け取った素材 ——
いずれもこのリポジトリには置かない。作者が別の場所で管理する。

**判断基準**: 製品を使う人・変更する人が読む必要があるか。なければ外に出す。

### 主張は証拠の範囲に留める

**調査が支持していないことを、確認済みの事実として書かない。**

README に「順序が頭の中にしかない」を共通課題として書いたが、R2 は自ら
「この粒度についての証拠は得られていない」と記録していた。外部レビューで指摘された。

- 未検証の一般化には「仮説」という語を含める
- 「どのツールも〜しない」ではなく「調査した範囲では見つからなかった」と書く
- 競合を評価するときは、`research/` の該当箇所を根拠にできるかを確認する。
  できないなら書かない

## 6. 環境

Node.js 24 未満はサポートしない(`node:sqlite` に 22.5 以上が必要。24 は Electron 内蔵の
Node と揃えるため)。

```bash
nvm use
npm install
npm run dev
npm run typecheck && npm test && npm run check   # check は非破壊。修正は npm run fix
```

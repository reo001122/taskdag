# CLAUDE.md

このリポジトリで作業するときの取り決め。**過去に実際に起きた失敗から作られている** ——
それぞれのルールには、そうしなかったときに何が起きたかが添えてある。

## 1. 文書の役割分担

**どの文書に何を書くかを間違えると、後から「どれが本当か」が分からなくなる。**
実際に、Project のモデルを変更したとき下流の文書だけ直して上流を放置し、
README と要件定義が正反対のことを言っている状態を作った。

### 公開するもの(このリポジトリ)

| 文書 | 書くこと | 書かないこと |
|---|---|---|
| `README.md` | **見知らぬ読者**が「自分向きか」を判断するための情報。問題設定、使い方の位置づけ、動作環境 | 内部の進捗、実装の詳細、判断の経緯 |
| `requirements.md` | **現行の仕様。** 何がどう振る舞うべきか、受け入れ条件つき | 実装方法、なぜその要件にしたかの長い経緯 |
| `design/*.md` | **どう作られているか。** 技術選定、ドメイン設計、永続化設計、規約 | 要件そのもの(requirements.md への参照で足りる) |
| `design/decisions.md` | **却下した案と、各判断が何を犠牲にしたか** | 現行仕様の説明(他の文書と重複させない) |
| `research/` | 一次情報にあたった調査。事実・根拠・未解決の問いを分けて書く | 結論や提案(調査は調査のまま置く) |
| `CONTRIBUTING.md` | 外部の人が変更を提案するために必要なこと | 内部の運用ルール |

### 公開しないもの(`../taskdag-workspace/`)

作業記録であって、製品の読者には不要なもの。

- `discussion/notes.md` — ブレストの経過、まだ確定していない仮説
- `vision.md` / `PRD.md` — **その時点の判断の記録。** 現行仕様ではない
- `qa-checklist.md` / `qa-results/` — 手動 QA の実施票と記録
- `distribution-and-usage.md` — 配布の準備メモ(署名の設定など)
- `inbox/` — 手書きスケッチ

**判断基準**: 「製品を使う人・変更する人が読む必要があるか」。
なければワークスペース側。迷ったら公開しない。

### 現行仕様は1つだけ

**`requirements.md` が唯一の規範文書。** 他の文書がこれと食い違ったら、他の文書が古い。

過去に vision / PRD / requirements の3つが並存し、それぞれ違うことを書いていた
(PRD は「AI が DB へ直接書き込む」前提のままだったが、requirements ではその方針が
逆転していた)。**どれが正かを判別できない状態**になったため、vision と PRD を
ワークスペースへ移した。同じことを繰り返さない。

## 2. 書き方の原則

### 主張は証拠の範囲に留める

**調査が支持していないことを、確認済みの事実として書かない。**

README に「順序が頭の中にしかない」「中断からの復帰にコストがかかる」を共通課題として
書いたが、R2 は自ら「この粒度についての証拠は得られていない」と記録していた。
外部レビューで指摘され、**仮説として明示する**書き方に直した。

- 調査で確かめたこと → そのまま書く
- 調査の範囲を超える推測 → **「仮説である」と明示する**
- 「どのツールも〜しない」のような断定 → 「調査した範囲では見つからなかった」に留める

### 他の製品を貶さない

競合名を出すのは、**読者が判断するために必要なとき**だけ。

「エッジは飾り」と書いたが、調査が支持していたのは「導出機能が文書に見つからなかった」
までだった。**その製品を使っている読者には、自分の選択を否定されたと読める。**

- 事実として何が見つかり、何が見つからなかったかを書く
- 「用途が違う」で済むなら、そう書く
- README の冒頭を他社名で始めない。**自分が解く問題から書く**

### なぜそうしたかを書く

コメントもコミットメッセージも、**何をしたかではなく、なぜそうしたか**。
何をしたかは差分が語る。

要件由来の判断には要件番号を書く(`// FR-3: 入力・出力の両方が複数の場合は再接続しない`)。
後から「この分岐は消していいのか」を判断するとき、要件に遡れる。

## 3. 実装の進め方

### ブランチとマージ

**`main` へ直接コミットしない。** feature ブランチで実装し、**動作確認を経てから**マージする。

型チェックもテストも通るのに画面が壊れている、という状態が何度も起きた ——
ノードがカーソルに付いてこない、タイトルが編集できない、枠線が3つの角で途切れている。
**自動チェックはこれらを1つも捕まえなかった。**

- ブランチ名: `feature/<何を変えるか>`
- マージは `--no-ff`(1つの検証単位が履歴に残る)
- コミットは Conventional Commits、英語、1コミット1論点

### レイヤー境界(最重要)

**`src/main/domain/` は外部への import を持たない。** Electron も `node:sqlite` も React も
参照しない、純粋な TypeScript。

これは好みではなく要件由来の制約。**すべての書き込み**(UI から、将来は MCP 経由で AI から)が
同じコードを通る必要があり、そうして初めて依存のルール・循環防止・Undo がどちらにも効く。
renderer にドメインロジックを書くと、AI 経由の操作がそれを迂回する。

```
src/main/
  domain/          ← ビジネスロジック。ここが唯一の真実。外部依存ゼロ
  db/              ← SQLite アクセス。app-service からのみ呼ばれる
  app-service.ts   ← domain と db の配線。書き込みは必ずここを通る
  ipc/             ← renderer との境界。app-service を呼ぶだけ
  mcp/             ← AI との境界。app-service を呼ぶだけ
```

renderer で何かの判定を書き直していたら、**それはドメインに属しており、呼ぶべきもの**。

### テストの方針

レイヤーによって変える。**一律に適用しない。**

| レイヤー | 方針 |
|---|---|
| `domain/` | **テストファースト。** 純粋関数で I/O がなく、要件がケースを列挙済み |
| `db/` | スキーマが固まってから書く |
| `ipc/` `mcp/` | 薄い変換層。結合テストを後から |
| `renderer/` | **単体テストを書かない。** 「読みやすいか」はテストで答えられない |

テスト名に要件番号を含める(`FR-3: 両側が複数の場合は自動再接続しない`)。
要件が変わったとき、影響を受けるテストを機械的に特定できる。

### 導出できるものは保存しない

Ready/Blocked も Project への所属も**計算する。DB に列を持たない。**

保存すると、更新漏れによる不整合が必ず起きる。導出なら**不整合が表現不可能**になる。
ただし SQL 側にも同じ導出を書くことになる(ビュー)ため、**ドメイン関数と一致することを
テストで確認する**。

### 確定していない状態を履歴に積まない

ドラッグ中の座標、入力中の1文字ごとの変更を Undo 履歴に積むと、履歴が中間状態で埋まり
Cmd+Z が使い物にならなくなる。**確定時(マウスを離す、フォーカスが外れる)に1回だけ**送る。

## 4. 変更したら文書も直す

**コードだけ直して文書を放置しない。** 実際に起きた例:

- Project を「タグ」から「領域」へ変えたとき、requirements と decisions は直したが
  README・vision・PRD が旧記述のまま残った
- 永続化設計の冒頭スキーマが v1 のままで、同じ文書のマイグレーション節が
  「v2 で削除した」と書いている状態になった

仕様に関わる変更をしたら、**その場で `requirements.md` を直す。**
設計判断が変わったら、**`design/decisions.md` に何を却下し何を失ったかを追記する。**

## 5. 環境

**Node.js 24 以上**が必須。`nvm use` を忘れると、既定の Node では `node:sqlite` がなく起動に失敗する。

```bash
nvm use
npm install     # ネイティブモジュールなし
npm run dev
npm run typecheck && npm test && npm run check
```
